import { RInput } from './RInput';

//@ts-ignore
export const RSearch = () => {
    const handleChange =() =>{
    
    }

    return (
        <RInput {...{handleChange, icon: "search", placeholder: "Search"}}/>
    )
}